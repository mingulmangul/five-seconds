extends ../base
include ../mixins/video

block main 
    div 
        if user.avatarUrl
            img(src="/"+user.avatarUrl, alt="Avatar", style="width:80px; height:80px; border-radious:50%;")
        else 
            p 😺
        h2=user.name 
        p=user.description
        if String(loggedInUser._id) === String(user._id)
            a(href=`/users/${user._id}/edit`) Edit Profile
    hr
    div 
        each video in user.videos 
            +video(video)
        else
            p No video
            a(href="/videos/upload") Go to upload &rarr;