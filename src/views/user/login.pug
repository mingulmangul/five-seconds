extends ../base

block main 
    form.form(method="post") 
        if messages.formError 
            div.messages__error--form
                i.fas.fa-exclamation-circle
                span=messages.formError
        label(for="email") Email
        input#email(type="email", name="email", required)
        label(for="password") Password
        input#password(type="password", name="password", required)
        div
            span.form__convert Don’t have an account?
            a.form__convert--link(href="/signup") Go to Sign up
        button.form__button(type="submit") Log In
        hr
        a.form__button.form__button--long(href="/login/github/start")
            i.fab.fa-github
            span Log In with GitHub